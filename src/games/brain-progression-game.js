import { brainGameEngine } from '..';
import generateRandomNum from '../utils';
import { cons } from 'hexlet-pairs';

const description = '';

const generateProgressionGameInfo = () => {
  const question = '';
  const correctAnswer = '';
  return cons(question, correctAnswer);
};

export default () => brainGameEngine(description, generateProgressionGameInfo);
